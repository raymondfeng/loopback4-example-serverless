import {Context, Callback} from 'aws-lambda';

/**
 * Signature of a AWS lambda function
 * @param event
 * @param context
 * @param callback
 */
// tslint:disable-next-line:no-any
export async function main(event: any, context: Context, callback: Callback) {}
